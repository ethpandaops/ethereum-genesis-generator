# Test configuration for minimal preset consensus-spec comparison.
# Uses a small validator count matching the minimal preset's MIN_GENESIS_ACTIVE_VALIDATOR_COUNT.
export PRESET_BASE=minimal
export NUMBER_OF_VALIDATORS=64
export GENESIS_TIMESTAMP=1700000000
