# Test configuration for mainnet consensus-spec comparison.
# Uses a small validator count so genesis generation completes quickly in CI.
export NUMBER_OF_VALIDATORS=64
export GENESIS_TIMESTAMP=1700000000
